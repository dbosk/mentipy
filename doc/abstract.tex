\texttt{mentipy} is a lightweight classroom-polling library that
replaces Mentimeter with a self-hosted solution.  It generates QR codes
and \LaTeX{} snippets for embedding audience questions in presentation
slides, and runs a local HTTP server that collects responses keyed by a
hash of each question.
